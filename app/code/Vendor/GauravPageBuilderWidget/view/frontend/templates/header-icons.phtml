<?php
$eyeIcon     = $block->getIconUrl('user.png');
$heartIcon   = $block->getIconUrl('wishlist.png');
$shuffleIcon = $block->getIconUrl('caviar_cart.png');

$objectManager = \Magento\Framework\App\ObjectManager::getInstance();

$checkoutSession = $objectManager->get(\Magento\Checkout\Model\Session::class);
$customerSession = $objectManager->get(\Magento\Customer\Model\Session::class);

$quote = $checkoutSession->getQuote();
$totalProducts = (int) $quote->getItemsQty();

$isLoggedIn = $customerSession->isLoggedIn();
?>

<ul class="flex_icon">
    <li>
        <a href="<?= $block->getUrl('customer/account'); ?>">
            <img src="<?= $eyeIcon; ?>" alt="User">
        </a>
    </li>

        <li>
            <a href="<?= $block->getUrl('wishlist'); ?>">
                <img src="<?= $heartIcon; ?>" alt="Wishlist">
                    <?php if ($isLoggedIn): ?>

                <span class="wishlist-count cart_count_badge">
                    <?= $block->getWishlistCount(); ?>
                </span>
                    <?php endif; ?>

            </a>
        </li>

    <li>
        <a href="<?= $block->getUrl('checkout/cart'); ?>">
            <img src="<?= $shuffleIcon; ?>" alt="Cart">
            <span class="cart-count cart_count_badge">
                <?= $totalProducts; ?>
            </span>
        </a>
    </li>
</ul>
